# Dockerfile for MCP MySQL Server
FROM node:18-alpine

# Install the MCP MySQL server
RUN npm install -g @modelcontextprotocol/server-mysql

# Create working directory
WORKDIR /app

# Expose port (if needed for network access)
EXPOSE 3001

# Default command to run the MCP server
CMD ["npx", "@modelcontextprotocol/server-mysql"]